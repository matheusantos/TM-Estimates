<div class="page-header">
    <h3><span class="glyphicon glyphicon-th-list"></span> Projetos</h3>
</div>
<div class="row">
    <div class="col-md-3">
        <a href="<?= $this->url(array('controller' => 'projeto', 'action' => 'novo'), null, true) ?>" class="btn btn btn-success" role="button">Novo</a>
    </div>
    <div class="col-md-3">
        <form action="#" method="get">
            <div class="input-group">
                <!-- USE TWITTER TYPEAHEAD JSON WITH API TO SEARCH -->
                <input class="form-control" id="system-search" name="q" placeholder="Search for" required>
                <span class="input-group-btn">
                    <button type="submit" class="btn btn-default"><i class="glyphicon glyphicon-search"></i></button>
                </span>
            </div>
        </form>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table table-list-search">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Título</th>
                    <th>Categoria</th>
                    <th>Dt. Criação</th>
                    <th>Dt. Finalização</th>
                    <th>Situação</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->dados AS $key => $prazo): ?>
                    <tr>
                        <td><?= $prazo['idProjeto'] ?></td>
                        <td><?= $prazo['Titulo'] ?></td>
                        <td><?= $prazo['Categoria'] ?></td>
                        <td><?= $prazo['DataCriacao'] ?></td>
                        <td><?= $prazo['DataFininalizacao'] ?></td>
                        <td><?= $prazo['Situacao'] ?></td>
                        <td><a href="<?= $this->url(array('controller' => 'editar', 'action' => 'index', 'idP'=>$prazo['idProjeto']), null, true) ?>">Editar</a></td>
                        <td><a href="<?= $this->url(array('controller' => 'editar', 'action' => 'excluir-pr', 'idP' => $prazo['idProjeto']), null, true) ?>">Excluir</a></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>   
    </div>
</div>
